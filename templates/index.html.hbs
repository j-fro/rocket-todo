<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>Test</title>
</head>

<body>
    <h1>Test</h1>
    <form class="" action="" method="post">
        <input type="text" name="name">
        <input type="hidden" name="complete" value="off">
        <input type="checkbox" name="complete" value="on">
        <input type="submit" value="Submit">
    </form>
    <ul>
        {{#each this as |t|}}
            <li data-id="{{t.id}}">{{t.name}}, 
                {{#if t.complete}}
                    Complete
                {{else}}
                    Incomplete
                {{/if}}</li>
        {{/each}}
    </ul>
</body>

</html>
